const HTGTheme={init(){const root=document.documentElement,key='hindPharmaTheme';const saved=localStorage.getItem(key);const theme=saved==='dark'||saved==='light'?saved:(matchMedia('(prefers-color-scheme: dark)').matches?'dark':'light');root.dataset.theme=theme;this.mount();this.update();},mount(){let btn=document.getElementById('themeToggle');if(!btn){const host=document.querySelector('.navActions')||document.querySelector('.top')||document.querySelector('.nav');if(!host)return;btn=document.createElement('button');btn.id='themeToggle';btn.className='htgThemeBtn';btn.type='button';btn.setAttribute('aria-label','Switch to dark mode');btn.innerHTML='<span class="htgThemeIcon" aria-hidden="true">☼</span>';host.appendChild(btn);}btn.onclick=()=>{const next=document.documentElement.dataset.theme==='dark'?'light':'dark';document.documentElement.dataset.theme=next;localStorage.setItem('hindPharmaTheme',next);this.update();};},update(){const dark=document.documentElement.dataset.theme==='dark';const btn=document.getElementById('themeToggle');if(!btn)return;const icon=btn.querySelector('.htgThemeIcon');if(icon)icon.textContent=dark?'☾':'☼';btn.setAttribute('aria-label',dark?'Switch to light mode':'Switch to dark mode');btn.setAttribute('title',dark?'Light mode':'Dark mode');}};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>HTGTheme.init(),{once:true});else HTGTheme.init();